# Reference outputs

This directory is reserved for reference outputs generated by the release profile.

Recommended workflow:

```matlab
cd code
run_all_figures("release")
```

The scripts write outputs to `code/matlab_outputs/S*/`. After generation, selected reference outputs can be copied here or archived directly through Zenodo.
